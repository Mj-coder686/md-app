# MD

一个本地优先、完全离线的 Markdown 阅读器，支持数学公式、代码高亮、目录、搜索和阅读样式设置。项目使用 Vite、TypeScript 与 Capacitor，可构建为 Web 应用或 Android 应用。

## 开发

```powershell
npm install
npm run dev
```

## 构建

```powershell
npm run build
npm run sync
```

`npm run sync` 会把 Web 构建结果同步到 Android 工程。
