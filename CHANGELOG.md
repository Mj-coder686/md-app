# 更新记录

## 1.4.0

以「书桌」为方向重做界面：纸白、墨黑和少量暖橙；首页呈现真实文档和继续阅读的位置。

- 重建首页结构，移除宣传卡片，增加独立的继续阅读入口。切换分类时该入口保持原位。
- 文档以两列封面呈现，较宽屏幕显示三列；PDF、Word 与 MD 各有封面排版，使用文字和矢量图形，不在首页读取所有 PDF 或绘制缩略图。
- 分类改为细线导航，突出文件名；长名称最多显示两行，完整名称仍提供给无障碍工具。
- 更新为折角书页图标，并统一应用启动页、阅读页、编辑器、目录、搜索、工具栏和设置的颜色与字体层次。
- 提供纸白、明亮和深色主题，保留原有文档、设置、双指缩放和离线阅读功能。
- 修复设置事件错误绑定到根页面、导致字号变为 NaN 的旧问题。读取旧设置时将无效字号恢复为默认值，不清除文档。

验证：检查 320、390、430 和 1100 像素宽度的布局、封面对齐、长名称、继续阅读的键盘操作、深色主题保存与字号调整；旧数据迁移、MD 编辑、PDF 绘制/翻页/缓存/手势、Word 图片与手势缩放、位置恢复及导入错误回滚检查通过。Android 真机外观和手势仍待验证。

安全状态：沿用 1.3 的 release 构建方式和旧证书以兼容覆盖升级。旧 1.2 的小米手机管家警报尚未有该安全引擎的新包复查结果，因此本版仍作为测试版发布，不宣称警报已消除。

## 1.3.0

本次以测试版发布：已知阅读问题已修复并通过模拟检查，小米手机管家对旧 1.2 安装包的 `a.gray.BulimiaTGen.f` 警报仍待新包真机复查，不能认定为误报。

- 应用更名为「轻阅」，重做淡蓝色文档架、分类入口、阅读工具栏、深色主题和书页图标。
- Word 与 MD 支持双指连续缩放，文字、表格、图片一起调整，工具栏保持大小；尽量保持当前段落的位置。底部按钮继续可用。
- PDF 手势过程中即时显示缩放预览，松手后绘制清晰页面；以双指中心调整查看位置，放大后可拖动查看，100% 时左右滑动翻页。
- 修复 PDF.js 使用较新接口造成「识别出页数但绘制失败」的兼容问题，主模块与工作线程同时切换到 legacy 构建，离线字体和解码资源继续随包提供。
- 缓存最多三张 PDF 页面，缓存像素总量设限；在适应宽度时延后预绘制下一页。新页面绘制完成前保留旧页面，减少空白闪动。
- PDF 文件之间复用解析线程，每份文档仍在退出时释放，避免每次重开都重新启动线程。
- 滚动期间即时更新内存中的位置，停止滚动后保存，退出或返回立即保存；打开文件不再重复写入文档正文，Word 卡片摘要避免解析全文。
- 修复正文重建和 PDF 绘制时覆盖已保存阅读位置的问题，保留旧版文档、位置和设置。
- 改为关闭调试标记的 Android release 构建。为兼容旧版覆盖安装，仍沿用原有 Android Debug 证书；这不等于安全引擎已放行。

验证：离线浏览器检查覆盖 PDF 实际像素、翻页缓存复用、快速连续翻页、手势即时缩放、Word 图片随手势放大、工具栏不随内容缩放、数据迁移、位置和比例恢复、编辑与搜索、导入错误回滚。另在主页面和 PDF 工作线程中移除部分新接口，复现旧版绘制失败后验证修复。尚无 Android 真机或小米安全引擎验证，不保证所有旧 WebView 或复杂 PDF 均兼容。

## 1.2.0

- 首页增加全部、Word、PDF、MD 分类，继续显示最近阅读的文件。
- 支持 PDF 原排版阅读：左右滑动、按钮翻页、页码跳转、放大缩小及双指缩放。
- 支持 Word（.docx）阅读，按手机屏幕重排标题、文字、表格和内嵌图片。
- MD 与 Word 增加阅读时的字号放大缩小。
- 自动记住每份文件的阅读位置和缩放比例，重新打开后继续阅读。
- 导入文件保存在本机，阅读资源随安装包提供，支持离线打开。
- 按需加载 PDF 文件，首页不会一次读入所有 PDF 的内容。
- 保留旧 MD 文档、设置、编辑、目录、搜索、公式和代码高亮功能。
- 相同名称和内容的 PDF、Word 再次导入时继续原记录。
- 修复 MD 编辑后立即返回时的保存时序问题和长摘要导致首页超出手机宽度的问题。
- 文件导入失败或空间不足时显示提示，保留已有文档。

边界：Word 暂支持 `.docx`，不支持旧 `.doc`；Word 复杂版式可能与原文件不同；PDF 保留原版面，不提供文字重排；加密 PDF 暂不支持；单个文件上限 80 MB。

验证：旧数据迁移、MD 编辑保存、分类、PDF 翻页/滑动/缩放、DOCX 标题/表格/图片/搜索、离线重开和阅读位置恢复、重复导入、损坏文件与空间不足回滚通过浏览器模拟检查。Android 构建通过，新旧 APK 签名一致。未进行 Android 真机测试。
